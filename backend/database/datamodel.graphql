type City {
  id: ID! @unique
  name: String! @unique
}

type School {
  id: ID! @unique
  name: String!
  email: String! @unique
  phone: String
  olympiadCood: User!
  pedagogyCoord: String
  director: String
  city: City!
  address: String
}

type Test {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  title: String!
  description: String!
  author: User!
}

type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  name: String!
  tests: [Test!]!
}
